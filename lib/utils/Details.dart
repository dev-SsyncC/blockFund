class Detail {
  String? key, name, account, description, date, accepted;
  Detail(
      {this.key,
      this.name,
      this.account,
      this.description,
      this.date,
      this.accepted});
}
